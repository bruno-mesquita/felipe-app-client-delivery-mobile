import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 40,
    minWidth: 120,
    alignItems: 'center',
    backgroundColor: 'white',
    borderRadius: 10,
    padding: 10,
    borderColor: '#000',
    marginBottom: 0,
    marginRight: 10,
    marginLeft: 10,
    elevation: 7,
    shadowColor: '#000',
    shadowOpacity: 0.9,
    shadowRadius: 5,
  },
});
